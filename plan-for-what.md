# ระบบบันทึกข้อมูลรถยนต์ (Car Record System)

## 1. วัตถุประสงค์ของระบบ
ระบบนี้ถูกออกแบบมาเพื่อช่วยบริหารจัดการข้อมูลรถยนต์อย่างง่ายและสะดวก โดยสามารถบันทึก ดู แก้ไข และลบข้อมูลรถยนต์ได้ในที่เดียว

## 2. เทคโนโลยีที่ใช้

### Backend
- Express.js + Node.js
- RESTful API
- PostgreSQL สำหรับจัดเก็บข้อมูล
- JWT, bcrypt, cors, dotenv สำหรับระบบความปลอดภัยและการตั้งค่า環境

### Frontend
- Vue 3 + Vite
- TailwindCSS + daisyUI สำหรับ UI ที่สวยงามและใช้งานง่าย
- Axios หรือ Fetch API สำหรับเชื่อมต่อกับ Backend

### Infrastructure
- Docker และ Docker Compose
- แยก Dockerfile สำหรับ Backend และ Frontend

## 3. ฟีเจอร์หลัก
- เพิ่มข้อมูลรถยนต์ใหม่
- ดูรายการรถยนต์ทั้งหมดและค้นหาข้อมูลได้
- แก้ไขข้อมูลรถยนต์ที่มีอยู่
- ลบข้อมูลรถยนต์ออกจากระบบ
- คัดกรองข้อมูลตามสี ยี่ห้อ ปีผลิต ราคา และสถานะ
- แสดงข้อมูลในรูปแบบ Card View และ Table View

## 4. ระบบ Authentication (JWT Login)
- ผู้ใช้สามารถสมัครสมาชิกและเข้าสู่ระบบด้วยอีเมลและรหัสผ่าน
- ระบบจะตรวจสอบข้อมูลผู้ใช้จากฐานข้อมูลก่อนอนุญาตให้เข้าสู่ระบบ
- เมื่อเข้าสู่ระบบสำเร็จ ระบบจะสร้าง JWT Token เพื่อใช้ยืนยันตัวตนในทุกคำขอที่ต้องการความปลอดภัย
- Token จะถูกเก็บใน Frontend เพื่อใช้ในการเรียก API ที่ต้องการสิทธิ์การเข้าถึง
- รหัสผ่านจะถูก Hash ด้วย bcrypt ก่อนบันทึกลงฐานข้อมูลเพื่อความปลอดภัย
- Backend จะมี Middleware ตรวจสอบ Token ก่อนอนุญาตให้เข้าถึง API ที่ต้อง login
- Frontend ใช้ Vue Router และ state management เพื่อป้องกันผู้ใช้ที่ยังไม่ได้ login จากเข้าถึงหน้าที่ต้องมีสิทธิ์
- ระบบสามารถรองรับการ logout โดยลบ Token และยกเลิก session ของผู้ใช้

## 5. การทำงานร่วมกัน
- ใช้ Git และ GitHub สำหรับควบคุมเวอร์ชัน
- แบ่ง Branch สำหรับงานฝั่ง Backend และ Frontend เพื่อให้ทีมทำงานร่วมกันได้สะดวกและลดความยุ่งยากจาก Code Conflict